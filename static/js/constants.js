// Application constants and configuration (non-module version)
const CONFIG = {
    API: {
        ENDPOINTS: {
            ANALYZE: '/analyze',
            STATUS: '/api/status',
            HISTORY: '/history',
            ANALYTICS: '/api/analytics',
            TRENDS: '/api/trends'
        },
        TIMEOUT: 30000,
        RETRY_ATTEMPTS: 3
    }
};

const NUTRITION_GOALS = {
    DAILY_CALORIES: 2000,
    DAILY_PROTEIN: 50,
    DAILY_CARBS: 300,
    DAILY_FAT: 70
};